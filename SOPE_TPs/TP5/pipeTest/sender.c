#include <stdio.h>
#include <string.h>

int main() {

    //char to_read[100];
    //scanf("%[^\n]s", to_read);
    // printf("-%s-\n", to_read);

    printf("Hello boy");
    fflush(stdout);

    return 0;
}